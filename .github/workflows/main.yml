name: my first workflow

on:
  push: 
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: first step 
        run: echo "welcome to the first workflow"
